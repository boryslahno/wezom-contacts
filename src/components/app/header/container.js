/*import { openModal, setAuthorized, fetchPersonalData } from '../../../store/actions/auth';
import { View } from './view';
import { connect } from 'react-redux';


const mapStateToProps = state => {
   return {
      isAuth: state.auth.isAuthorized,
      name: state.auth.personalInformation.fullName?.fullName,
      avatar: state.auth.personalInformation.avatar?.url,
   }
};

const mapDispatchToProps = {
   openModal,
   setAuthorized,
   fetchPersonalData,
}


const Header = connect(mapStateToProps, mapDispatchToProps)(View);

export { Header };*/